import { StyleSheet } from "react-native";

export const detailNavigationStyles = StyleSheet.create({
  detailActionRow: {
    flexDirection: "row",
    gap: 8,
  },
  detailActionBtn: {
    flex: 1,
  },
});
